monitor testEvent {
	constant string eventChannel := "channel1";
	
	action onload() {
		monitor.subscribe(eventChannel);
		
		on all p1() as event1 {
			sendAction(event1);
		}
		
	}
	
					
			
	action sendAction(p1 event1) {
		send e1(event1.airport, "found!") to eventChannel;
	}
}

