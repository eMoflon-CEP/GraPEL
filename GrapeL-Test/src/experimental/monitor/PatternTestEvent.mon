monitor testEvent {
	const string eventChannel := "channel1";
	
	action onload() {
		monitor.subscribe(eventChannel);
		
		on all EventP1Match as p1 {
			send EventE1(p1.airport, "found!") to eventChannel;
		}
		
	}
}